services:
  - type: web
    name: taskapp-backend
    env: java
    rootDir: backend/taskapp
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    plan: free
